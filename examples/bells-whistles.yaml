jobs:
  docker-build-only:
    steps:
    - checkout: {}
    - docker/build:
        debug: true
  goodbye:
    steps:
    - run:
        command: echo "goodbye world"
  hadolint: {}
  hello:
    steps:
    - run:
        command: echo "hello world"
orbs:
  docker: circleci/docker@0.5.20
version: "2.1"
workflows:
  orbs-example:
    jobs:
    - docker-build-only:
        requires:
        - hello
        - hadolint
    - goodbye:
        requires:
        - hadolint
        - docker-build-only
    - hadolint: {}
    - hello: {}
